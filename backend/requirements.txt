# ============================================================
# AI Travel Planner - Backend Dependencies
# ============================================================

# HelloAgents 框架
hello-agents[protocols]>=0.2.4

# HelloAgents 依赖（显式添加以确保 Docker 构建成功）
huggingface_hub>=0.20.0

# FastAPI 和 Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# 数据验证和配置
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP 客户端
httpx>=0.27.0
aiohttp>=3.10.0
requests>=2.31.0

# 数据库和认证（Supabase）
supabase>=2.0.0
postgrest>=0.10.0

# 环境变量管理
python-dotenv>=1.0.0

# CORS 和文件上传支持
python-multipart>=0.0.9

# 日志
loguru>=0.7.0

# MCP 相关
fastmcp>=2.0.0
uv>=0.8.0

# 日期时间处理
python-dateutil>=2.8.2

# JSON 处理（Python 3.11+ 内置，但显式声明以确保兼容性）
# json - 标准库

# 并发处理（标准库）
# asyncio - 标准库
# threading - 标准库
# concurrent.futures - 标准库

# 类型提示（标准库）
# typing - 标准库

# 其他标准库依赖
# os - 标准库
# pathlib - 标准库
# uuid - 标准库
# warnings - 标准库
# queue - 标准库

